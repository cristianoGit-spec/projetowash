# ============================================================================
# CONFIGURACAO NETLIFY - SITE ESTATICO PURO
# Sistema Quatro Cantos - Gestão Empresarial
# Repository: https://github.com/cristiano-superacao/projetowash
# ============================================================================

[build]
  publish = "web"
  command = "echo 'Site estático - sem build necessário'"
  
# Ignorar node_modules e arquivos desnecessários
[build.environment]
  NODE_VERSION = "18"

# Redirecionar todas as rotas para index.html (SPA - Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers de seguranca
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "no-referrer"

# Cache para assets estaticos
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

